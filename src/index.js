import React from 'react';
import ReactDOM from 'react-dom';
import App from './app'; // Main React component

// Render your React app to the root element
ReactDOM.render(<App />, document.getElementById('root'));
